'use client'

interface GoogleReviewsProps {
  placeId?: string
  className?: string
}

export default function GoogleReviews({ className = '' }: GoogleReviewsProps) {
  // Retirado a pedido do cliente (removendo a seção de avaliações que falhava na key)
  return null
}



